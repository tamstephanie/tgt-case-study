export const DropdownStyle =  (theme) => ({
    dropdown: {
        "& .MuiSelect-root": {
            fontSize: "1.15rem",
            width: "100%"
        }
    },
    optionItem: {
        fontSize: "1.15rem"
    }
});