export const UserActionBarStyle = (theme) => ({
    actionBar: {
        "& .MuiAppBar-root": {
            // boxShadow: "none"
        },
        "& .MuiContainer-root": {
            padding: "0px 0px"
        },
        "& .clickable-icon": {
            cursor: "pointer"
        }
    }
});